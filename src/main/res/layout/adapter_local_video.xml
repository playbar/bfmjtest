<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:descendantFocusability="blocksDescendants">
    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:padding="10dp"
        android:orientation="horizontal">
    <ImageView
        android:id="@+id/local_file_image"
        android:layout_width="72dp"
        android:layout_height="55dp"
        android:scaleType="fitXY"
        android:src="@drawable/img_default_3n_cross"/>

    <LinearLayout
        android:layout_width="0.0dp"
        android:layout_weight="1.0"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="5dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/local_file_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:singleLine="true"
            android:text="@null"
            android:textColor="#333333"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/local_file_describe"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="15dp"
            android:text="@null"
            android:textSize="12sp"
            android:textColor="@color/prompt_color"/>

    </LinearLayout>

    <Button
        android:id="@+id/local_file_btn"
        style="@style/parent_btn_style"
        android:background="@drawable/corner_purple_bg"
        android:textColor="@color/theme_main_color"
        android:visibility="gone"
        android:text="预览" />
    </LinearLayout>
    <View
        android:id="@+id/view_divider"
        style="@style/divider_line" />
</LinearLayout>